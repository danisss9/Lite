# Tests the WPT survey (--suite wpt --survey <dir>) must NOT run because they crash the
# whole process rather than failing cleanly: deep JS recursion overflows the CLR stack
# (an uncatchable StackOverflowException — the same Jint engine limitation documented for
# test262 tco-*). Substring path match. Format: <path-substring> | <reason>
#
# These are engine limitations, not Lite layout/DOM bugs. Re-evaluate if Jint gains proper
# tail calls or a configurable recursion limit.

dom/events/EventTarget-dispatchEvent.html | re-entrant dispatchEvent recurses infinitely: Lite's dispatch has no "dispatch flag" guard (should throw InvalidStateError), so a listener that re-dispatches the same event overflows the CLR stack
dom/events/remove-all-listeners.html | re-entrant dispatch from a once-listener recurses infinitely: Lite doesn't remove the once listener before invoking it under re-entrancy, so it re-fires and overflows the CLR stack
