# Curated CSS 2.1 reftest manifest: <test path> | <reference path> [expected-fail: reason]
# Both sides are rendered by Lite at 600x600 and pixel-compared.

# ---- harness sanity (Phase 0 gate) ----
css21/sanity.html | css21/sanity-ref.html

# ---- Phase 3: length units (in/pt/pc/rem resolve to px equivalents) ----
css21/units.html | css21/units-ref.html

# ---- Phase 3: margin collapsing (negative margins per §8.3.1) ----
css21/margin-collapse-neg.html | css21/margin-collapse-neg-ref.html

# ---- Phase 3: currentColor resolves to the element's color ----
css21/currentcolor.html | css21/currentcolor-ref.html

# (% margin/padding width-resolution is covered by LayoutTests geometry asserts, which avoid
#  margin-collapse interactions that make an authored pixel reftest ambiguous.)

# block-in-inline (inline broken around a block → block flow) is covered by a deterministic
# geometry test in LayoutTests (BlockInInline_*). The upstream box-display/block-in-inline-*
# reftests are table-based and differ by ~2% on <td> border anti-aliasing, not the structure;
# survey count rose 3→24 on box-display from the inline→block promotion.

# ---- Phase 3: stacking contexts (Appendix E flattening — z-index across nesting levels) ----
css21/stacking-flatten.html | css21/stacking-flatten-ref.html

# ---- Phase 3: small items — @import, attr() in content, clip: rect() ----
css21/import-main.html | css21/import-ref.html
css21/attr-content.html | css21/attr-content-ref.html
css21/clip-rect.html | css21/clip-rect-ref.html
# (list-style-image url parsing covered by CssWideKeywordTests; initial/unset by CssWideKeywordTests)

# ---- Phase 3: medium items — direction:rtl, ::first-letter, table caption, empty-cells ----
css21/rtl-align.html | css21/rtl-align-ref.html
css21/first-letter-color.html | css21/first-letter-color-ref.html
css21/caption-top.html | css21/caption-top-ref.html
css21/caption-bottom.html | css21/caption-bottom-ref.html
css21/empty-cells-hide.html | css21/empty-cells-hide-ref.html

# ---- Phase A: nested CSS counters — counters(name, sep) joins the scope stack (§12.4.1) ----
css21/counters-nested.html | css21/counters-nested-ref.html

# ---- Phase A: text-align justify (equal inter-word gaps == flex space-between) ----
css21/justify.html | css21/justify-ref.html

# ---- Phase A.4: block-in-inline §9.2.1.1 — inline's vertical margins ignored; self-collapsing
#      block-in-inline collapses through itself + ancestors (upstream WPT, white-canvas default) ----
css/CSS2/box-display/block-in-inline-vertical-margins-on-span-ignored.html | css/CSS2/box-display/block-in-inline-vertical-margins-on-span-ignored-ref.html
css/CSS2/box-display/block-in-inline-self-collapsing-only-child.html | css/CSS2/box-display/block-in-inline-self-collapsing-only-child-ref.html

# ---- Phase B: interactive elements — <datalist> is display:none per the UA stylesheet ----
css21/datalist-hidden.html | css21/datalist-hidden-ref.html

# ---- A solid border side must be FILLED as a rectangle, not stroked as a centred line: a stroke
#      antialiases the long edges and no longer matches an equivalent background fill. The CSS 2.1
#      suite compares bordered boxes against background-filled ones constantly, so the fringe alone
#      failed ~44 margin-padding-clear tests. Guarded here rather than in Lite.Tests because a
#      synthetic bordered box rasterises identically either way at integer coordinates. ----
css21/border-vs-background.html | css21/border-vs-background-ref.html

# ---- §16.2: 'text-align' aligns the whole LINE BOX, so inline-level BOXES move with it, not
#      just text. It had one consumer (the text painter), which meant the suite's own reference
#      files — which position swatches with text-align — rendered wrong, so the affected tests
#      could never match no matter what the test side did. ----
css21/text-align-inline-box.html | css21/text-align-inline-box-ref.html

# ---- §10.3.2/§10.6.2: a replaced box takes its intrinsic size for any 'auto' dimension. Only
#      the inline path did; block-level images filled the container and every out-of-flow image
#      collapsed to zero height. Guarded end-to-end here because the failure needs a real
#      decoded image, which the synthetic unit harness does not have. ----
css21/replaced-block-size.html | css21/replaced-block-size-ref.html

# ---- §9.6.1: a position:fixed box must be painted. The dedicated fixed pass called back into
#      the paint entry point that skips fixed boxes, so it was a no-op and no fixed box ever
#      painted — invisible in the suite because a blank test matches a blank reference. ----
css21/fixed-position-paint.html | css21/fixed-position-paint-ref.html

# ---- Phase 3 additions (full anonymous-box splitting, z-index:auto subtlety...) ----

# ---- §5.12.1: ::first-letter is a real inline box, so its font metrics and 'line-height' size
#      the line box it sits on. Styling it at paint time (re-drawing the first characters larger)
#      left the line box sized for the parent's font. Upstream css/CSS2/selectors 93 -> 495. ----
css/CSS2/selectors/first-letter-punctuation-001.xht
css/CSS2/selectors/first-letter-nested-005.xht

# ---- A background must paint whole device pixels, like the replaced content the suite's own
#      references use in its place. Anti-aliasing a box that lands on a fractional y left a
#      half-covered fringe row, which alone blew the pixel budget in ~370 normal-flow tests. ----
css/CSS2/normal-flow/width-050.xht

# ---- §15.7: 'font-size' computes to a length and descendants inherit THAT. Re-resolving the
#      inherited specified value ("2em") at every level doubled the size again each time — the
#      Gecko-imported anonymous-table tests reached 1024px and painted glyphs across the page. ----
css/CSS2/tables/table-anonymous-objects-015.xht

# ---- A negative 'border-width' is invalid, so the side falls back to the initial 'medium'
#      wherever it still has a visible style. ----
css/CSS2/borders/border-top-width-001.xht

# ---- §9.5 rule 7: a line box too narrow for its content shifts down past the float instead of
#      overflowing it, and lines below the float get the full width back. ----
css/CSS2/floats/floats-wrap-bfc-with-margin-001.html

# ---- §10.3.7 / §10.6.4: with left/top auto an absolutely positioned box takes its static
#      position — where it would have been in normal flow — not its containing block's origin. ----
css/CSS2/abspos/hypothetical-box-dynamic.html
